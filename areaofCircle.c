#include <stdio.h>

int main() {
    float radius = 12;                  // radius of the circle
    float area = 3.14 * radius * radius; // formula for area

    printf("The Area of Circle is %f", area); // print result
    return 0;
}
